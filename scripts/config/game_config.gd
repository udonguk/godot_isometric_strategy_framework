extends Node

## 게임 전역 설정
## UI/Logic 분리 원칙: 모든 비주얼 설정을 여기서 중앙 관리

# ============================================================
# 건물 상태별 색상 (modulate 적용)
# ============================================================

## 일반 상태 (감염되지 않음)
const COLOR_NORMAL: Color = Color.WHITE

## 감염 진행 중 (노란색)
const COLOR_INFECTING: Color = Color.YELLOW

## 완전 감염됨 (빨간색)
const COLOR_INFECTED: Color = Color.RED


# ============================================================
# 텍스처 크기 설정
# ============================================================

## 타일 텍스처 크기 (픽셀)
const TILE_SIZE: Vector2i = Vector2i(32, 32)

## 건물 텍스처 크기 (픽셀)
const BUILDING_SIZE: Vector2i = Vector2i(32, 32)

## 유닛 텍스처 크기 (픽셀) - 타일의 70%
const UNIT_SIZE: Vector2i = Vector2i(22, 22)


# ============================================================
# 그리드 시스템 설정
# ============================================================

## 그리드 셀 크기 (논리적 단위)
const GRID_CELL_SIZE: Vector2i = Vector2i(1, 1)


# ============================================================
# 맵 시스템 설정
# ============================================================

## 맵 너비 (그리드 단위)
const MAP_WIDTH: int = 20

## 맵 높이 (그리드 단위)
const MAP_HEIGHT: int = 20


# ============================================================
# 내비게이션 시스템 설정
# ============================================================

## Navigation 유효성 검증 허용 오차 (픽셀 단위)
## 그리드 좌표를 월드 좌표로 변환한 후, NavigationServer가 반환한 가장 가까운 점과의 거리가 이 값 이하면 유효한 것으로 판단
const NAVIGATION_TOLERANCE: float = 8.0
