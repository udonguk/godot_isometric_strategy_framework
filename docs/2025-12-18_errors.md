# 2025-12-18 개발 중 발생한 오류 정리

> **목적**: 실수하기 쉬운 오류 유형을 정리하여 향후 동일한 실수 방지

---

## 📋 오류 요약

| # | 오류 유형 | 원인 | 해결 시간 |
|---|---------|------|----------|
| 1 | 좌표 시스템 불일치 | GridSystem vs TileMapLayer | 30분 |
| 2 | SOLID 원칙 위반 (DIP) | 고수준이 저수준에 직접 의존 | 1시간 |
| 3 | Autoload 접근 오류 | 씬 트리 미등록 + 파서 인식 문제 | 1시간 |

---

## 🔴 오류 #1: 좌표 시스템 불일치

### 증상
```
클릭한 건물: Grid (0, 0) 예상
실제 출력: Grid (0, 2)
```

건물을 클릭했을 때 표시되는 그리드 좌표가 실제 타일 좌표와 일치하지 않음.

### 원인

**두 개의 다른 좌표 변환 시스템이 혼재:**

1. **GridSystem (커스텀 변환)**
```gdscript
// scripts/map/grid_system.gd
static func grid_to_world(grid_pos: Vector2i) -> Vector2:
    var world_x = (grid_pos.x - grid_pos.y) * (TILE_WIDTH / 2.0)
    var world_y = (grid_pos.x + grid_pos.y) * (TILE_HEIGHT / 2.0)
    return Vector2(world_x, world_y)
```

2. **TileMapLayer (Godot 내장)**
```gdscript
// ground_tilemaplayer.tscn
ground_layer.map_to_local(grid_pos)  // Godot 내장 변환
```

**문제**: 같은 그리드 좌표를 넣어도 다른 월드 좌표 반환!

### 해결 방법

**✅ TileMapLayer를 단일 진실 공급원(Single Source of Truth)으로 사용**

```gdscript
// BuildingManager (Before - 잘못됨)
var world_pos = GridSystem.grid_to_world(grid_pos)  // ❌ 커스텀 변환

// BuildingManager (After - 올바름)
var world_pos = GridSystem.grid_to_world(grid_pos)  // ✅ 내부적으로 TileMapLayer 사용
```

**GridSystem 리팩토링:**
```gdscript
// scripts/map/grid_system.gd
var ground_layer: TileMapLayer = null

func initialize(tile_layer: TileMapLayer):
    ground_layer = tile_layer

func grid_to_world(grid_pos: Vector2i) -> Vector2:
    return ground_layer.map_to_local(grid_pos)  // TileMapLayer 사용!
```

### 교훈

**🎯 핵심 원칙: "모든 entity는 ground 타일 위에 정확히 배치되어야 한다"**

- ✅ **단일 진실 공급원**: 좌표 변환은 TileMapLayer만 사용
- ✅ **Godot 내장 우선**: 직접 구현보다 Godot 내장 기능 활용
- ❌ **커스텀 변환 금지**: 타일 시스템과 다른 변환 공식 사용 금지

### 예방 방법

**체크리스트:**
- [ ] 좌표 변환이 필요한가? → GridSystem 사용
- [ ] GridSystem이 TileMapLayer를 사용하는가?
- [ ] 여러 변환 시스템이 혼재하지 않는가?

---

## 🔴 오류 #2: SOLID 원칙 위반 (의존성 역전 위반)

### 증상
```
BuildingManager가 TileMapLayer를 직접 참조
→ 향후 TileMapLayer 변경 시 모든 매니저 수정 필요
```

### 원인

**고수준 모듈이 저수준 모듈에 직접 의존 (DIP 위반)**

```gdscript
// building_manager.gd (잘못된 설계)
class BuildingManager:
    var ground_layer: TileMapLayer  // ❌ 저수준에 직접 의존!

    func create_building(grid_pos):
        var world_pos = ground_layer.map_to_local(grid_pos)  // ❌
```

**구조:**
```
BuildingManager (고수준)
    ↓ 직접 의존 ❌
TileMapLayer (저수준 - Godot 내장)
```

**문제점:**
1. TileMapLayer 변경 → BuildingManager도 수정 필요
2. EnemyManager, ItemManager도 모두 같은 문제
3. 결합도 높음 (Tight Coupling)
4. 테스트 어려움

### 해결 방법

**✅ 추상화 레이어(GridSystem) 도입**

```gdscript
// building_manager.gd (올바른 설계)
class BuildingManager:
    // ground_layer 참조 제거! ✅

    func create_building(grid_pos):
        var world_pos = GridSystem.grid_to_world(grid_pos)  // ✅ 추상화에 의존
```

**구조:**
```
[고수준 - 게임 로직]
  BuildingManager
  EnemyManager
  ItemManager
       ↓
[추상화 레이어]
  GridSystem ← 단일 책임!
       ↓
[저수준 - Godot 내장]
  TileMapLayer
```

**장점:**
- ✅ TileMapLayer 변경 → GridSystem만 수정
- ✅ BuildingManager는 변경 불필요
- ✅ 결합도 낮음 (Loose Coupling)
- ✅ 테스트 용이 (GridSystem을 Mock으로 교체 가능)

### 교훈

**🎯 SOLID 원칙 - Dependency Inversion Principle (DIP)**

> "고수준 모듈은 저수준 모듈에 의존하지 않고, 추상화에 의존해야 함"

**핵심 규칙:**
1. ✅ 매니저는 **절대** Godot 내장 타입 직접 참조 금지
2. ✅ 모든 좌표 변환은 **GridSystem**을 통해서만
3. ✅ 모든 설정값은 **GameConfig**를 통해서만
4. ✅ 각 매니저는 **하나의 도메인**만 담당

### 예방 방법

**코드 리뷰 체크리스트:**
```gdscript
// ❌ 안티 패턴 감지
var ground_layer: TileMapLayer          // 매니저가 TileMapLayer 직접 참조?
ground_layer.map_to_local(grid_pos)     // TileMapLayer 직접 호출?

// ✅ 올바른 패턴
GridSystem.grid_to_world(grid_pos)      // 추상화 레이어 사용
GameConfig.TILE_SIZE                    // 설정 중앙화
```

**새 매니저 추가 시:**
- [ ] TileMapLayer, Sprite2D 등 Godot 내장 타입을 직접 참조하는가? → ❌
- [ ] GridSystem, GameConfig 같은 추상화를 사용하는가? → ✅
- [ ] 단일 책임 원칙을 준수하는가? (하나의 도메인만) → ✅

---

## 🔴 오류 #3: Autoload 싱글톤 접근 오류

### 증상

**3-1. Identifier not declared in the current scope**
```
Error: Identifier "GridSystem" not declared in the current scope.
at: building_manager.gd:67
```

**3-2. Cannot call non-static function**
```
Parser Error: Cannot call non-static function "initialize()"
on the class "res://scripts/map/grid_system.gd" directly.
Make an instance instead.
at: test_map.gd:66
```

### 원인

**여러 가지 원인이 복합적으로 작용:**

#### 원인 1: 씬 트리에 미등록
```gdscript
// test_map.gd (잘못됨)
var building_manager = BuildingManager.new()
// add_child() 없음! ❌
building_manager.initialize()

// building_manager.gd
func create_building():
    var pos = GridSystem.grid_to_world()  // ❌ 에러!
```

**Godot 규칙**: Autoload는 **씬 트리에 있는 노드만** 접근 가능!

#### 원인 2: Godot 에디터 미재시작
```
1. project.godot에 Autoload 추가
2. Godot 에디터 재시작 안 함 ❌
3. 파서가 여전히 GridSystem을 클래스로 인식
```

#### 원인 3: 파서 인식 문제
- Godot 에디터를 재시작해도 파서가 Autoload를 인식 못함
- VS Code 같은 외부 에디터의 LSP 문제

### 해결 방법

**해결 1: 씬 트리에 추가**
```gdscript
// test_map.gd (올바름)
var building_manager = BuildingManager.new()
add_child(building_manager)  // ✅ 씬 트리에 추가!
building_manager.initialize()
```

**해결 2: Godot 에디터 완전 재시작**
```
1. Godot 에디터 완전 종료 (X 버튼)
2. 5초 대기
3. Godot 에디터 다시 실행
4. 프로젝트 열기
```

**중요**: "Reload Project"가 아니라 **완전 종료 후 재시작**!

**해결 3: get_node() 명시적 접근 (최종 해결책)**
```gdscript
// 모든 파일에서
// Before
GridSystem.initialize(ground_layer)
GridSystem.grid_to_world(grid_pos)

// After (항상 작동!)
var grid_system = get_node("/root/GridSystem")
grid_system.initialize(ground_layer)
grid_system.grid_to_world(grid_pos)
```

**`get_node("/root/...")`는 Godot의 표준 Autoload 접근 방식입니다.**

### 교훈

**🎯 Godot Autoload 접근 규칙**

#### 규칙 1: 씬 트리 등록 필수
```gdscript
// ❌ 잘못된 패턴
var manager = MyManager.new()
// add_child() 없음!

// ✅ 올바른 패턴
var manager = MyManager.new()
add_child(manager)  // 반드시 씬 트리에 추가!
```

#### 규칙 2: 에디터 재시작 필수
```
project.godot 수정 후:
1. Godot 에디터 완전 종료
2. 다시 시작
3. Autoload 인식됨
```

#### 규칙 3: 명시적 접근 권장
```gdscript
// 방법 1 (이상적이지만 가끔 안 됨)
GridSystem.method()

// 방법 2 (항상 작동!)
var grid_system = get_node("/root/GridSystem")
grid_system.method()
```

### 예방 방법

**매니저/시스템 생성 체크리스트:**
```gdscript
// 1단계: 생성
var my_manager = MyManager.new()

// 2단계: 씬 트리에 추가 (필수!)
add_child(my_manager)

// 3단계: 초기화
my_manager.initialize()

// 4단계: Autoload 접근 가능 확인
// - get_node("/root/AutoloadName") 사용 권장
```

**Autoload 추가 체크리스트:**
1. [ ] project.godot에 Autoload 추가
2. [ ] Godot 에디터 완전 재시작 (X 버튼)
3. [ ] Project Settings → Autoload 탭에서 확인
4. [ ] `get_node("/root/...")` 방식으로 접근

---

## 📚 학습한 원칙 정리

### 1. Single Source of Truth
- ✅ 좌표 변환은 TileMapLayer만 사용
- ✅ 여러 변환 시스템 혼재 금지

### 2. Dependency Inversion (DIP)
- ✅ 고수준 → 추상화 → 저수준
- ✅ 매니저는 Godot 내장 타입 직접 참조 금지

### 3. Single Responsibility (SRP)
- ✅ GridSystem: 좌표 변환만
- ✅ BuildingManager: 건물 생성/관리만

### 4. Godot Autoload 패턴
- ✅ 매니저는 씬 트리에 추가
- ✅ `get_node("/root/...")` 명시적 접근
- ✅ project.godot 수정 후 에디터 재시작

---

## 🎯 빠른 참조 가이드

### 좌표 변환이 필요할 때
```gdscript
// ❌ 직접 변환 금지
var world_pos = ground_layer.map_to_local(grid_pos)

// ✅ GridSystem 사용
var grid_system = get_node("/root/GridSystem")
var world_pos = grid_system.grid_to_world(grid_pos)
```

### 새 매니저 추가할 때
```gdscript
// 1. 생성
var manager = MyManager.new()

// 2. 씬 트리에 추가 (필수!)
add_child(manager)

// 3. 초기화
manager.initialize()
```

### Autoload 접근할 때
```gdscript
// 항상 작동하는 방식
var singleton = get_node("/root/SingletonName")
singleton.method()
```

### 의존성 체크할 때
```gdscript
// ❌ 안티 패턴
var ground_layer: TileMapLayer
var sprite: Sprite2D

// ✅ 올바른 패턴
// Godot 내장 타입 참조 없음!
// GridSystem, GameConfig 같은 추상화만 사용
```

---

## 🔗 관련 문서

- **SOLID 원칙 상세**: `CLAUDE.md` → "SOLID 원칙 준수" 섹션
- **Autoload 규칙**: `CLAUDE.md` → "Autoload 싱글톤 접근 규칙" 섹션
- **타일 시스템**: `docs/design/tile_system_design.md`
- **코드 컨벤션**: `docs/code_convention.md`

---

## ✅ 체크리스트 (향후 개발 시)

### 코드 작성 전
- [ ] 좌표 변환이 필요한가? → GridSystem 사용
- [ ] Godot 내장 타입을 직접 참조하는가? → 추상화 사용
- [ ] 매니저가 여러 책임을 가지는가? → 분리 필요

### 매니저 생성 시
- [ ] `new()` 후 `add_child()` 했는가?
- [ ] Autoload를 `get_node("/root/...")` 방식으로 접근하는가?
- [ ] 단일 책임 원칙을 준수하는가?

### Autoload 추가 시
- [ ] project.godot에 등록했는가?
- [ ] Godot 에디터를 완전히 재시작했는가?
- [ ] Project Settings → Autoload에서 확인했는가?

### 디버깅 시
- [ ] 좌표가 올바른가? → GridSystem vs TileMapLayer 일치 확인
- [ ] "not declared" 에러? → 씬 트리 등록 확인
- [ ] "Cannot call non-static" 에러? → get_node() 사용

---

**마지막 업데이트**: 2025-12-18
**작성자**: Claude Code Assistant
**프로젝트**: isometric_strategy_framework (Godot 4.5)
